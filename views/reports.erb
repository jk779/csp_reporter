<!DOCTYPE html>
<html>
<head>
  <title>CSP Reports</title>
  <style>
    body { font-family: Arial, sans-serif; }
    table { width: 100%; border-collapse: collapse; }
    th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
    th { background-color: #f2f2f2; }
  </style>
</head>
<body>
  <h1>CSP Reports</h1>
  <table>
    <tr>
      <th>ID</th>
      <th>Report</th>
      <th>Received At</th>
    </tr>
    <% @reports.each do |report| %>
      <tr>
        <td><%= report[0] %></td>
        <td><pre><%= JSON.pretty_generate(JSON.parse(report[1])) %></pre></td>
        <td><%= report[2] %></td>
      </tr>
    <% end %>
  </table>
</body>
</html>
